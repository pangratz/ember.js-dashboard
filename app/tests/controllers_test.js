require('dashboard/controllers');

module("Dashboard.GitHubController");

test("it exists",
function() {
    ok(Dashboard.GitHubController, "controller exists");
});

module("Dashboard.TwitterController");

test("it exists",
function() {
    ok(Dashboard.TwitterController, "controller exists");
});

module("Dashboard.StackOverflowController");

test("it exists",
function() {
    ok(Dashboard.StackOverflowController, "controller exists");
});

module("Dashboard.RedditController");

test("it exists",
function() {
    ok(Dashboard.RedditController, "controller exists");
});
