<ul class="unstyled timeline">
	{{#each questions}}
	  {{ago "last_activity_date" isSeconds=true}}
		<li>{{score}}/{{answer_count}} <a {{bindAttr href="link"}} >{{{title}}}</a></li>
	{{else}}
		loading latest questions on StackOverflow ...
	{{/each}}
</ul>